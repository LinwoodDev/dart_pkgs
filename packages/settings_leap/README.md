# SettingsLeap

Flutter helpers for building searchable, expandable settings views.

`settingsleap` models settings as a tree of pages and custom entries. Apps keep
control over localization, icons, navigation, and page bodies while the package
provides flattening, matching, and Material search/result widgets.
